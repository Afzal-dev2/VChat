<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.malkinfo.vchat">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VChat"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.malkinfo.vchat.ChatlistActivity"
            android:exported="false" />
        <activity android:name="com.malkinfo.vchat.ChatActivity" />
        <activity android:name="com.malkinfo.vchat.SetupProfileActivity" />
        <activity android:name="com.malkinfo.vchat.OTPActivity" />
        <activity android:name="com.malkinfo.vchat.VerificationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.malkinfo.vchat.MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
    </application>

</manifest>